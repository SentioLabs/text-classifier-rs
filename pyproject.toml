[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "text-classifier"
version = "0.1.0"
description = "Classify text fields for translation eligibility"
requires-python = ">=3.10"

[tool.maturin]
features = ["python"]
module-name = "text_classifier"
